{
  "name": "paikko",
  "version": "0.0.0",
  "private": true,
  "description": "Point at the bug. The agent fixes it. (monorepo root)",
  "workspaces": [
    "packages/*",
    "examples/*"
  ],
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "build:contract": "npm run build -w @paikko/contract"
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  }
}
